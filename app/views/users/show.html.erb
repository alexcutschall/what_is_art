<% present @user do |user| %>
  <div class="landscape-picture">
    <%= image_tag image_url('forest.jpg'), class: 'profile-landscape' %>
  </div>
  <%= image_tag (user.profile_pic), class: 'user-profile-pic' %>
  <div class="simple-box">
    <p class="simple-text">Favorites: <%= user.favorites.count %></p>
  </div>

  <div class="profile-container">
    <div class="profile-column">
      <div class="name-box">
        <%= user.username %><br>
        <%= link_to "Edit Profile", edit_user_path(user) %>
      </div>
      <div class="quote-box">
        <h2>Quote of the Day</h2><br>
        <%= @quote.text %><br><br>
        <%= @quote.author %><br>
      </div>

      <% cache do %>
        <div class="quiz-box">
          <%= link_to "Quiz of the Day", quiz_of_the_day_path %>
        </div>
      </div>

      <div class="profile-column">
        <div class="favorites-box">
          <h2>Favorites</h2>
          <%= link_to image_tag(image_url('favorite.png'), class: 'profile-favorite'), user_favorite_path(user.id) %>
        </div>

        <div class="gallery-box">
          <h2>Gallery</h2>
          <%= link_to image_tag(image_url('gallery.png'), class: 'profile-gallery'), gallery_path %>
        </div>

        <div class="calendar-box">
          <%= button_to("I'm Feeling Artsy", sample_artwork_path, method: 'get', class: "im-feeling-lucky-btn") %>
        </div>
      <% end %>
    </div>
  </div>
<% end %>
